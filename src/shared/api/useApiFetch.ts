export function useApiFetch(path: String, options: Object) {
    // const appConfig = useAppConfig()
    // let headers = {}
    // const token = useCookie('token');
    // if (token.value) {
    //     headers['authorization'] = token;
    // }
    // return useFetch(appConfig.api.baseUrl + path, {
        
    // });
    console.log(options)
    return path;
}